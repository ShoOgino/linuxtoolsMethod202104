    @Override
    public String getValue(Object obj) {
        TreeElement e = (TreeElement) obj;
        int i = e.getCalls();
        if (i == -1) {
            return ""; //$NON-NLS-1$
        }
        String ret = String.valueOf(i);
        return ret;
    }

