	/*
	 * (non-Javadoc)
	 * @see com.st.dataviewers.abstractviewers.AbstractSTViewer#setColumnOrder(int[])
	 */
	protected void setColumnOrder(int[] order) {
		getViewer().getTable().setColumnOrder(order);
	}

