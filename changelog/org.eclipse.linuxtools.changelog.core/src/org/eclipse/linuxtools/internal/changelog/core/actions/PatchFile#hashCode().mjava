    @Override
    public int hashCode() {
        int hash = resource.hashCode();
        for(int i=0; i<this.pranges.size();i++) {
            hash += pranges.get(i).hashCode();
        }
        return hash;
    }

