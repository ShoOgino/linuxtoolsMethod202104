    private boolean isDate(String inputStr) {
        try {
            return isoDate.parse(inputStr) != null;
        } catch (ParseException e) {
            // Don't care
        }
        return false;
    }

