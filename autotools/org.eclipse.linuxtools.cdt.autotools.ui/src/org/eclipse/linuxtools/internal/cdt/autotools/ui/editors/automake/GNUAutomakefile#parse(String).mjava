	public void parse(String name) throws IOException {
		FileReader stream = new FileReader(name);
		try {
			parse(name, stream);
		} finally {
			if (stream != null) {
				stream.close();
			}
		}
	}

