	public void testIf0() {
		String text = "# comment\n"+
		"\tif true; then\n" +
		"\t\tfoo;\n"+
		"\tfi\n";
		AutoconfElement tree = parse(text);
		assertTreeStructure(tree, new String[] { "if" });
	}

