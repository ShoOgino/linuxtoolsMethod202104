	// Look for Automake conditionals which optionally
	// activate or disable a Makefile rule command.
	public static boolean isAutomakeCommand(String line) {
		return line.matches("^@[a-z_A-Z0-9]+@\\t.*$");
	}

