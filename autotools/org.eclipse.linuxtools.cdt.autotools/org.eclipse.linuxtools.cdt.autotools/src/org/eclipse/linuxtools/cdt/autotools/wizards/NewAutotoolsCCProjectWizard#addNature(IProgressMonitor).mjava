	protected void addNature(IProgressMonitor monitor) throws CoreException {
		monitor.beginTask("", 4);
		monitor.subTask(AutotoolsPlugin.getResourceString(MSG_ADD_NATURE));
		ManagedCProjectNature.addManagedNature(newProject, new SubProgressMonitor(monitor, 1));
		monitor.subTask(AutotoolsPlugin.getResourceString(MSG_ADD_BUILDER));
		ManagedCProjectNature.addManagedBuilder(newProject, new SubProgressMonitor(monitor, 1));
		monitor.subTask(AutotoolsPlugin.getResourceString(MSG_ADD_NATURE));
		AutotoolsProjectNature.addAutotoolsNature(newProject, new SubProgressMonitor(monitor, 1));
		monitor.subTask(AutotoolsPlugin.getResourceString(MSG_ADD_BUILDER));
		AutotoolsProjectNature.addAutotoolsBuilder(newProject, new SubProgressMonitor(monitor, 1));
		monitor.done();
	}

