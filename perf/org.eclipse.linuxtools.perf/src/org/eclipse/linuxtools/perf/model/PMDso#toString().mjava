	public String toString() {
		String prefix = "";
		if (percent != -1)
			prefix += percent + "% in ";
		if (kernel == true)
			prefix += "[k] ";
		if (path != null) {
			return prefix + name + " (at " + path + ")";
		}
		return prefix + name;
	}

