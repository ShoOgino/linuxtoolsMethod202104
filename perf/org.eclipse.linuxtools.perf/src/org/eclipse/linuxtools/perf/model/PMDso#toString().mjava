	public String toString() {
		String prefix = "";
		if (getPercent() != -1)
			prefix += getPercent() + "% in ";
		if (kernel == true)
			prefix += "[k] ";
		if (path != null) {
			return prefix + getName() + " (at " + path + ")";
		}
		return prefix + getName();
	}

