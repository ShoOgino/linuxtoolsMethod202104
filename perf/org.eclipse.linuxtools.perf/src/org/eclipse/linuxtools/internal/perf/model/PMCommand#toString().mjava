    @Override
    public String toString() {
        String prefix = "";
        if (getPercent() != -1) {
            prefix = getPercent() + "%  (" + getFormattedSamples() + " samples) in ";
        }
        return prefix + getName();
    }

