    @Override
    public String toString() {
        String prefix = ""; //$NON-NLS-1$
        if (getPercent() != -1) {
            prefix = getPercent() + "% in "; //$NON-NLS-1$
        }
        return prefix + path;
    }

