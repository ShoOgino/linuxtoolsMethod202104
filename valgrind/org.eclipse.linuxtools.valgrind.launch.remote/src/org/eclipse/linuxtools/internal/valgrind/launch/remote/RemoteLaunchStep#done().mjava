	public void done() {
		if (channel.getState() != IChannel.STATE_OPEN) {
			return;
		}
        try {
        	if (!steps.isEmpty()) {
        		steps.removeFirst().start();
        	}
        }
        catch (Throwable x) {
            channel.terminate(x);
        }
	}

