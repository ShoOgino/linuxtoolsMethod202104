	private static String getScaledUnit(TimeUnit unit, int scaling) {
		String name;
		int ix = scaling;
		switch (unit) {
		case BYTES:
			name = byteUnits[ix];
			break;
		case INSTRUCTIONS:
			name = instrUnits[ix];
			break;
		default:
			name = secondUnits[ix];
			break;
		}
		return name;
	}

