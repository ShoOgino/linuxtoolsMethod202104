	/**
	 * @param cm
	 *            Chart Model
	 */
	public void renderModel(Chart cm) {
		// if ( cm == null )
		// {
		// return;
		// }
		this.cm = cm;

		if (canvas != null && !canvas.isDisposed()) {
			updateBuffer();
			canvas.redraw();
		}
	}

