	private static int getMultiplier(TimeUnit unit) {
		int mult;
		switch (unit) {
		case BYTES:
			mult = BYTE_MULT;
			break;
		case INSTRUCTIONS:
			mult = INSTR_MULT;
			break;
		default:
			mult = MS_MULT;
			break;
		}
		return mult;
	}

