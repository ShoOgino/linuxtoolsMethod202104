        @Override
        public Object getPropertyValue(Object id) {
            if (NOTE.equals(id)) {
                return fNote;
            } else if (DESCRIPTION.equals(id)) {
                return fEventDescription;
            }
            return null;
        }

