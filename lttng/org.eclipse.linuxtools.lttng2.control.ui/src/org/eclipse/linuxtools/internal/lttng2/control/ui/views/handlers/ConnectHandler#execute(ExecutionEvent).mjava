    @Override
    public Object execute(ExecutionEvent event) throws ExecutionException {
        fLock.lock();
        try {
            fTargetNode.connect();
        } finally {
            fLock.unlock();
        }
        return null;
    }

