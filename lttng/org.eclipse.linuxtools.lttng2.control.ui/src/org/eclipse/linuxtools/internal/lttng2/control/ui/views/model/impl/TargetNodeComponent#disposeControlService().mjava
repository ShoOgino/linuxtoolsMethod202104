    private void disposeControlService() {
        fService = null;
        final ICommandShell shell = fShell;
        if (shell != null) {
            shell.disconnect();
            fShell = null;
        }
    }

