	public int getSelectedIndex() {
		int idx = -1;
		for (int i = 0; i < _data._items.length; i++) {
			Item item = (Item) _data._items[i];
			if (item._selected) {
				idx = i;
				break;
			}
		}
		return idx;
	}

