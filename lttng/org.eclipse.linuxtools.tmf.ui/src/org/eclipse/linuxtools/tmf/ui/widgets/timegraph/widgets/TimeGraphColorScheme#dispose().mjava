    /**
     * Dispose this color scheme
     */
    public void dispose() {
        for (int i = 0; i < fColors.length; i++) {
            Utils.dispose(fColors[i]);
            fColors[i] = null;
        }
    }

