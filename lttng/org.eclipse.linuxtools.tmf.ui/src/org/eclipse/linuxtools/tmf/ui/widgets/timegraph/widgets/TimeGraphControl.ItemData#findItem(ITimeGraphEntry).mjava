        Item findItem(ITimeGraphEntry entry) {
            if (entry == null) {
                return null;
            }

            for (int i = 0; i < fItems.length; i++) {
                Item item = fItems[i];
                if (item.fTrace == entry) {
                    return item;
                }
            }

            return null;
        }

