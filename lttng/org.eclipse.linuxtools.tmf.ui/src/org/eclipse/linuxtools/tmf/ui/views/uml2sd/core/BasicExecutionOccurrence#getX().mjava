    @Override
    public int getX() {
        if (lifeline == null)
            return 0;
        return lifeline.getX() + Metrics.getLifelineWidth() / 2 - Metrics.EXECUTION_OCCURRENCE_WIDTH / 2;
    }

