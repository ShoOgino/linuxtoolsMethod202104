    public void setTopIndex(int idx) {
        idx = Math.min(idx, _data._expandedItems.length - countPerPage());
        idx = Math.max(0,  idx);
        _topIndex = idx;
        redraw();
    }

