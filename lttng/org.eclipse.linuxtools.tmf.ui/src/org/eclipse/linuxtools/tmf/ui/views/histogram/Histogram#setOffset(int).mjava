    /**
     * Set the bucket display offset
     *
     * @param offset
     *            the bucket display offset
     * @since 2.2
     */
    protected void setOffset(final int offset) {
        fOffset = offset;
    }

