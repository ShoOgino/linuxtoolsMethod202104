    @Override
    public void run() {
        if (getView() == null) {
            return;
        }
        FilterListDialog dialog = new FilterListDialog(getView(), fProvider);
        dialog.open();
    }

