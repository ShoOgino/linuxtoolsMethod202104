    @Override
    public int contentsToViewY(int _y) {
        if (printing) {
            int v = Math.round(printerY * printerZoom);
            return _y - v;
        }
        return _y - getContentsY();
    }

