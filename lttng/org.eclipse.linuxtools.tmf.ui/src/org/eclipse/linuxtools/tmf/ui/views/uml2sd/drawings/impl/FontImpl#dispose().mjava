    /*
     * (non-Javadoc)
     * @see org.eclipse.linuxtools.tmf.ui.views.uml2sd.drawings.IFont#dispose()
     */
    @Override
    public void dispose() {
        if (font != null) {
            font.dispose();
        }
    }

