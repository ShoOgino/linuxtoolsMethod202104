    @Override
    public void dispose() {
        KeyBindingsManager.getInstance().remove(this.getSite().getId());
        disposeZoomActions();
        fPrintActionHandler.dispose();
        super.dispose();
    }

