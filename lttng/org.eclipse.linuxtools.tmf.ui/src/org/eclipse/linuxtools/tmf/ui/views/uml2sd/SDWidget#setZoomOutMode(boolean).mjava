    /**
     * Sets the zoom out mode.
     *
     * @param value
     *          The mode value to set.
     */
    public void setZoomOutMode(boolean value) {
        if (value) {
            setZoomInMode(false);
        }
        fZoomOutMode = value;
    }

