    @Override
    public void widgetDisposed(DisposeEvent e) {
        if (tooltip != null)
            tooltip.dispose();
        super.removeDisposeListener(this);
        for (int i = 0; i < col.length; i++)
            col[i].dispose();
    }

