    /*
     * (non-Javadoc)
     * @see org.eclipse.linuxtools.tmf.ui.views.histogram.Histogram#clear()
     */
    @Override
    public synchronized void clear() {
        if (fZoom != null) {
            fZoom.stop();
        }
        super.clear();
    }

