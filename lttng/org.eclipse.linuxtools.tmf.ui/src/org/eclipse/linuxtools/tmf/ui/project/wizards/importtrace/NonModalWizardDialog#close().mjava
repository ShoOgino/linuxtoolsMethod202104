    @Override
    public boolean close() {
        for(HelperThread t : fHelpers){
            t.interrupt();
        }
        for(HelperThread t : fHelpers){
            try {
                t.join(100);
            } catch (InterruptedException e) {
            }
        }
        return super.close();
    }

