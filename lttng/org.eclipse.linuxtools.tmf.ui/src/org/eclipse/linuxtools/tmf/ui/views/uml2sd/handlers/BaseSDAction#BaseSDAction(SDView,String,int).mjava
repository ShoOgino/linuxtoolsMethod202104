    /**
     * Constructor
     * @param view
     *          a sequence diagram view reference
     * @param text
     *          The action text
     * @param style
     *          The style
     */
    protected BaseSDAction(SDView view, String text, int style) {
        super(text, style);
        fView = view;
    }

