	public void focusLost(FocusEvent e) {
		_isInFocus = false;
		if (0 != _dragState) {
			setCapture(false);
			_dragState = 0;
		}
		redraw();
		getDisplay().removeFilter(SWT.MouseWheel, mouseScrollFilterListener);
	}

