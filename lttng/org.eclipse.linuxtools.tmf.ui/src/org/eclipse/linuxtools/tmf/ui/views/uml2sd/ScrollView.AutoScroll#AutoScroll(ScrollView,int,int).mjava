        /**
         * Constructor.
         *
         * @param sv
         *            ScrollView object to use
         * @param dx
         *            X delta
         * @param dy
         *            Y delta
         */
        public AutoScroll(ScrollView sv, int dx, int dy) {
            scrollView = sv;
            deltaX = dx;
            deltaY = dy;
        }

