    /**
     * Constructor
     *
     * @param view a sequence diagram view reference
     */
    public MoveSDLeft(SDView view) {
        super();
        setId(ID);
        setActionDefinitionId(ID);
        fView = view;
    }

