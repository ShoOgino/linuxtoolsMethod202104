    /**
     * Cancel all current requests and dispose the handler.
     */
    public synchronized void dispose() {
        fCurrentTask = null;
        fTimer.cancel();
        fTimer.purge();
    }

