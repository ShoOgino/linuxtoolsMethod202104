        @Override
        public void run() {
            Display.getDefault().asyncExec(new Runnable() {
                @Override
                public void run() {
                    if (sv_.isDisposed())
                        return;
                    sv_.dragX += dx_;
                    sv_.dragY += dy_;
                    sv_.scrollBy(dx_, dy_);
                }
            });
        }

