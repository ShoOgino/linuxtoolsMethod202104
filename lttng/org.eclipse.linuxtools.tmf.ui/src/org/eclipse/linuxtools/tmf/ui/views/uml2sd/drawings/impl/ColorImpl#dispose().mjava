    /*
     * (non-Javadoc)
     * @see org.eclipse.linuxtools.tmf.ui.views.uml2sd.drawings.IColor#dispose()
     */
    @Override
    public void dispose() {
        if ((col != null) && (manageColor)) {
            col.dispose();
        }
    }

