    public void print() {
        SDPrintDialog sdPrinter = new SDPrintDialog(this.getShell(), this);
        try {
            if (sdPrinter.open() != 0)
                return;
        } catch (Exception e) {

        }

        printUI(sdPrinter.getDialogUI());
    }

