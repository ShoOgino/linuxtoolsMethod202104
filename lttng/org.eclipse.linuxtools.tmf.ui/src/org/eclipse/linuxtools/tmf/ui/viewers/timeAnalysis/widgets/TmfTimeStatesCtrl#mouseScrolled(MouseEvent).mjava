    @Override
    public void mouseScrolled(MouseEvent e) {
        if (!_isInFocus)
            return;
        if (e.count > 0) {
            zoom(true);
        } else if (e.count < 0) {
            zoom(false);
        }
    }

