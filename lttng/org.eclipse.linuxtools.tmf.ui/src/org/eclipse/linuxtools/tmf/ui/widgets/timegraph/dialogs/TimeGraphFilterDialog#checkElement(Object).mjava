    private void checkElement(Object element) {
        Object e = element;
        while (e != null) {
            fViewer.setChecked(e, true);
            e = fContentProvider.getParent(e);
        }
    }

