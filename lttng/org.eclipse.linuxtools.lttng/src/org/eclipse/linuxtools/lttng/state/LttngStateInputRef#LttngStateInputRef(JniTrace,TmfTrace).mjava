	// ========================================================================
	// Constructor
	// ========================================================================
	LttngStateInputRef(JniTrace trace, TmfTrace log) {
		this.trace = trace;
		this.log = log;
	}

