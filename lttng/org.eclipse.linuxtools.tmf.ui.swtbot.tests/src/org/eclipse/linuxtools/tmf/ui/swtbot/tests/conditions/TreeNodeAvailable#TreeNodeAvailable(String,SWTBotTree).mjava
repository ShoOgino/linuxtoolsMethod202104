    /**
     * Is a tree node available
     *
     * @param name
     *            The name of the node
     * @param tree
     *            the swtbotTree
     */
    public TreeNodeAvailable(String name, SWTBotTree tree) {
        fTree = tree;
        fName = name;
    }

