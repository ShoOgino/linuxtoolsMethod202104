    /**
     * Clears the stack content.
     */
    public void clearStack() {
        fStack.clear();

        // Reset the processed and matched events counter
        fProcessedEvents = 0;
        fMatchedEvents = 0;
    }

