    /**
     * Resets all.
     */
    public void resetMatches() {
        fMatch.clear();
        fInverseMatch.clear();

        fStack.clear();

        // Reset the processed and matched events counter
        fProcessedEvents = 0;
        fMatchedEvents = 0;
    }

