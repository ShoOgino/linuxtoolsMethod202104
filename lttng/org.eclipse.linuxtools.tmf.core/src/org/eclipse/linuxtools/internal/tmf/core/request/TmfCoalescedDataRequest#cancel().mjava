    @Override
    public void cancel() {
        for (ITmfDataRequest request : fRequests) {
            if (!request.isCompleted()) {
                request.cancel();
            }
        }
        super.cancel();
    }

