    @Override
    public int hashCode() {
        int result = 17;
        for (int i = 0; i < fContexts.size(); i++) {
            result = 37 * result + fContexts.get(i).hashCode();
        }
        return result;
    }

