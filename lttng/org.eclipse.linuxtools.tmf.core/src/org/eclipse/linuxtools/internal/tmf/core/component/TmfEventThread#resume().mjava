    /**
     * Resume the thread
     */
    public synchronized void resume() {
        isPaused = false;
        synchronized (object) {
            object.notifyAll();
        }
        TmfCoreTracer.traceRequest(fRequest, "RESUMED"); //$NON-NLS-1$
    }

