    /**
     * Copy constructor
     *
     * @param location
     *            Other location to copy
     * @since 2.0
     */
    public CtfLocation(CtfLocationData location) {
        setLocation(location);
    }

