    /**
     * @return
     */
    private CtfIterator getIterator() {
        if( !nextIter.hasNext()){
            nextIter = fIterators.listIterator(0);
        }
        return nextIter.next();
    }

