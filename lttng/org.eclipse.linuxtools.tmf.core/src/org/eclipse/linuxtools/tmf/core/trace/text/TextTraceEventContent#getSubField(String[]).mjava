    @Override
    public ITmfEventField getSubField(String[] names) {
        // There are no sub fields
        if (names.length == 1) {
            return getField(names[0]);
        }
        return null;
    }

