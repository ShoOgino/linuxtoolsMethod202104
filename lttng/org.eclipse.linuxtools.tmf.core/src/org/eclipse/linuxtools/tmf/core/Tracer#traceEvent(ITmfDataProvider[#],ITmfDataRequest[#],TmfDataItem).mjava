    public static void traceEvent(ITmfDataProvider<?> provider, ITmfDataRequest<?> request, TmfDataItem data) {
        String message = ("[EVT] Provider=" + provider.toString() + ", Req=" + request.getRequestId() + ", Event=" + data
                .toString());
        trace(message);
    }

