    @Override
    public List<Long> getValue() {
        List<Long> retVal = new ArrayList<Long>();
        for( Long l : longValues){
            retVal.add(l);
        }
        return retVal;
    }

