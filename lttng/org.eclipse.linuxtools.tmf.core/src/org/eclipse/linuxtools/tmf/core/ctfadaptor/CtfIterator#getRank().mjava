    @Override
    public long getRank() {
        final CtfTmfEvent current = getCurrentEvent();
        if (current != null) {
            return getCurrentEvent().getRank();
        }
        return 0;
    }

