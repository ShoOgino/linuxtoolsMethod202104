	@Override
	public boolean matches(TmfEvent event) {
		for (ITmfFilterTreeNode node : getChildren()) {
			if (node.matches(event)) {
				return true ^ fNot;
			}
		}
		return false & fNot;
	}

