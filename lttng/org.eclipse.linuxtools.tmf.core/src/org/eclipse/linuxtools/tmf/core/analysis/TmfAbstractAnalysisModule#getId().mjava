    @Override
    @NonNull
    public String getId() {
        String id = fId;
        if (id == null) {
            id = new String(this.getClass().getCanonicalName());
            fId = id;
        }
        return id;
    }

