        /**
         * Send a signal
         *
         * @param signal
         *            main signal to send
         */
        public void sendSignal(TmfSignal signal) {
            broadcast(signal);
        }

