        @Override
        public synchronized void handleStarted() {
            super.handleStarted();
            synchronized (monitor) {
                monitor.notifyAll();
            }
        }

