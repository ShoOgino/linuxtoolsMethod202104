    private static void verify(int a, long t1, long t2) {
        if (t1 != t2) {
            nbErrors++;
            System.err.println("Check failed for attribute " + a + ": " + t1
                    + " vs " + t2);
        }
    }

