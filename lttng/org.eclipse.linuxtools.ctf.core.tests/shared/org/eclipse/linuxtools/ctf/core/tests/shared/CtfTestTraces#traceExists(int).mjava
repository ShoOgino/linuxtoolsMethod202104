    private static boolean traceExists(int idx) {
        if (testTraces[idx] != null) {
            return true;
        }
        try {
            getTestTrace(idx);
        } catch (CTFReaderException e) {
            return false;
        }
        return true;
    }

