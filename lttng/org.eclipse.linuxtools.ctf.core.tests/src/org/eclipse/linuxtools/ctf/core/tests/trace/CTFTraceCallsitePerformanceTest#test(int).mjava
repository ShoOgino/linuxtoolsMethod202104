    /**
     * @param callsiteSize
     */
    private void test(int callsiteSize) {
        addCallsites(callsiteSize);
        long ns = testMain();
        System.out.println( "perf ( " + callsiteSize + ", " + ns + ")");
    }

