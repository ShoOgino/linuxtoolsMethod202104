    /**
     * Run the String getLabel(long) method test.
     */
    @Test
    public void testGetLabel() {
        long i = 0;
        String result = fixture.getLabel(i);

        assertNull(result);
    }

