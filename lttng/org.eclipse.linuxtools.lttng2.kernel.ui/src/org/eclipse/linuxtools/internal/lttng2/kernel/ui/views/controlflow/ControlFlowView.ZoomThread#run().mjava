        @Override
        public void run() {
            if (fEntryList == null) {
                return;
            }
            for (ControlFlowEntry entry : fEntryList) {
                if (fMonitor.isCanceled()) {
                    return;
                }
                zoom(entry, fMonitor);
            }
            redraw();
        }

