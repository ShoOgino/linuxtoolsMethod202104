        @Override
        public void run() {
            if (fZoomEntryList == null) {
                return;
            }
            for (ControlFlowEntry entry : fZoomEntryList) {
                if (fMonitor.isCanceled()) {
                    break;
                }
                zoom(entry, fMonitor);
            }
        }

