        @Override
        public void run() {
            if (fEntryList == null) {
                return;
            }
            for (ControlFlowEntry entry : fEntryList) {
                if (fCancelled) {
                    return;
                }
                zoom(entry);
            }
            redraw();
        }

