    /**
     * Constructor.
     * @param viewer
     *            A reference to the listener's viewer.
     */
    public GraphPaintListener(AbstractViewer view) {
        super(view);
    }

