	/*
	 * (non-Javadoc)
	 * @see java.lang.Object#hashCode()
	 */
	@Override
	public int hashCode() {
		int hash = 1;
		for (int i = 0; i < fArray.length; ++i) {
			hash = hash ^ fArray[i];
		}
		return hash;
	}

