    /**
     * Constructor.
     * @param parentShell
     *            The parent shell.
     */
    @SuppressWarnings("nls")
    public AbstractDialog(Shell parentShell) {
        this(parentShell, "", "");
    }

