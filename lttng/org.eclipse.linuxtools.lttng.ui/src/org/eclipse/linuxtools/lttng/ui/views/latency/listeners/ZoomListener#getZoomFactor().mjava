    /**
     * Returns the zoom factor.
     * @return The zoom factor.
     */
    public int getZoomFactor() {
        if (fZoomFactor < 1)
            return 1;
        else
            return fZoomFactor;
    }

