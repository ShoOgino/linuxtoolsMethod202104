    public long getHorLastEventTime() {
        fLock.lock(); 
        try {
            return fHorDistributionData.getLastEventTime();
        } finally {
            fLock.unlock();
        }
    }

