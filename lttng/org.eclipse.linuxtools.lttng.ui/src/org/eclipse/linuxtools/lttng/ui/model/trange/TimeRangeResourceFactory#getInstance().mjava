	// ========================================================================
	// Create instance
	// =======================================================================
	/**
	 * Factory singleton
	 * 
	 * @return
	 */
	public static TimeRangeResourceFactory getInstance() {
		if (instance == null) {
			instance = new TimeRangeResourceFactory();
		}
		return instance;
	}

