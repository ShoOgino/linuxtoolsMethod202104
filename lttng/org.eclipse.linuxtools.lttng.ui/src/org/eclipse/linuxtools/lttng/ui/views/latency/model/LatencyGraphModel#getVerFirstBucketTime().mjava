    public long getVerFirstBucketTime() {
        fLock.lock(); 
        try {
            return fVerDistributionData.getFirstBucketTime();
        } finally {
            fLock.unlock();
        }
    }

