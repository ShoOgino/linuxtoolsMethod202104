	/**
	 * @return
	 */
	public int getWidth() {
		if (width == 0) {
			TraceDebug
					.debug("Unexpected width value of 0 pixels, returning default"); //$NON-NLS-1$
			return DEFAULT_WIDTH;
		}

		return width;
	}

