    /**
     * Dispose of controller
     */
    public void dispose() {
        if (fEventRequest != null && !fEventRequest.isCompleted()) {
            fEventRequest.cancel();
        }
        fProvider = null;
    }

