    public long getVerLastEventTime() {
        fLock.lock(); 
        try {
            return fVerDistributionData.getLastEventTime();
        } finally {
            fLock.unlock();
        }
    }

