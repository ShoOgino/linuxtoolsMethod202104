    @Override
    public void mouseScrolled(MouseEvent e) {
        if ((mouseScrollFilterListener == null) || _dragState != DRAG_NONE)
            return;
        if (e.count > 0) {
            zoom(true);
        } else if (e.count < 0) {
            zoom(false);
        }
    }

