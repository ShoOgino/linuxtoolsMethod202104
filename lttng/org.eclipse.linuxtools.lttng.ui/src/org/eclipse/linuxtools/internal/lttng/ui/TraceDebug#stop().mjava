    public static void stop() {
        if (fCFVfile != null) {
            fCFVfile.close();
            fCFVfile = null;
        }

        if (fRVfile != null) {
            fRVfile.close();
            fRVfile = null;
        }

        if (fSVfile != null) {
            fSVfile.close();
            fSVfile = null;
        }
    }

