    public boolean canRead(int index, int length) {
        if (buf == null) {
            return false;
        }

        if ((index + length) > (buf.capacity() * BIT_CHAR)) {
            return false;
        }
        return true;
    }

