    /**
     * Trace constructor.
     *
     * @param path
     *            Filesystem path of the trace directory.
     * @throws IOException
     */
    public CTFTrace(String path) throws CTFReaderException {
        this(new File(path));
    }

