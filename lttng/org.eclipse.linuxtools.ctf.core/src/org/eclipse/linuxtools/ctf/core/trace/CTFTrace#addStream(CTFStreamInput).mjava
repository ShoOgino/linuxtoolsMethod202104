    private void addStream(CTFStreamInput s) {

        /*
         * add the stream
         */
        CTFStream stream = s.getStream();
        fStreams.put(stream.getId(), stream);

        /*
         * index the trace
         */
        s.setupIndex();
    }

