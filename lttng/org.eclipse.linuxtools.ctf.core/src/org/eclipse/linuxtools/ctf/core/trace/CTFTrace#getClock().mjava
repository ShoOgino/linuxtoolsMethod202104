    public CTFClock getClock(){
        if( clocks.size() == 1 )
        {
            String key = (String) clocks.keySet().toArray()[0];
            return clocks.get(key);
        }
        return null;
    }

