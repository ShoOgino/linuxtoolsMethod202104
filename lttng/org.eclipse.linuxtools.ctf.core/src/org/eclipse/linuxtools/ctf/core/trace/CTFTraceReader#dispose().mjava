    /**
     * Dispose the CTFTraceReader
     *
     * @since 2.0
     */
    public void dispose() {
        for (StreamInputReader reader : fStreamInputReaders) {
            if (reader != null) {
                reader.dispose();
            }
        }
        fStreamInputReaders.clear();
    }

