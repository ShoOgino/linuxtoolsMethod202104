    /**
     * Sets the byte buffer
     *
     * @param buf
     *            the byte buffer
     */
    public void setByteBuffer(ByteBuffer buf) {
        fBuffer = buf;
        if (buf != null) {
            fBuffer.order(fByteOrder);
        }
        clear();
    }

