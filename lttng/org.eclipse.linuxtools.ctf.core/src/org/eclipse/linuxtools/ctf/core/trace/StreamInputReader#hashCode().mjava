    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = (prime * result) + fId;
        result = (prime * result)
                + ((fStreamInput == null) ? 0 : fStreamInput.hashCode());
        return result;
    }

