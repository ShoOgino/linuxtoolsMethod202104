    /**
     * Sets the order of the buffer.
     *
     * @param order
     *            The order of the buffer.
     */
    public void setByteOrder(ByteOrder order) {
        fByteOrder = order;
        if (fBuffer != null) {
            fBuffer.order(order);
        }
    }

