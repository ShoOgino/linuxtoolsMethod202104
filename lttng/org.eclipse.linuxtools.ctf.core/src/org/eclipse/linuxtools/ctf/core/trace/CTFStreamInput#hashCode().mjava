    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = (prime * result) + ((fFile == null) ? 0 : fFile.hashCode());
        return result;
    }

