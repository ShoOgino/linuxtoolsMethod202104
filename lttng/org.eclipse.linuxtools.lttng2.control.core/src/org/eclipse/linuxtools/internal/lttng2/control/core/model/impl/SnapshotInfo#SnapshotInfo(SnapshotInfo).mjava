    /**
     * Copy constructor
     * @param other - the instance to copy
     */
    public SnapshotInfo(SnapshotInfo other) {
        super(other);
        fPath = other.fPath;
        fId = other.fId;
        fIsStreamed = other.fIsStreamed;
    }

