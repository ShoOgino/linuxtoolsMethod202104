	// Tracers
	private static void trace(String message) {
		System.out.println(message);
		try {
			if (fTraceLog != null) {
				fTraceLog.write(message);
				fTraceLog.newLine();
				fTraceLog.flush();
			}
		} catch (IOException e) {
			e.printStackTrace();
		}
	}

