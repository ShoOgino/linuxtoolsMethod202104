    /**
     * Clears the experiment
     */
    @Override
	public void dispose() {
    	for (ITmfTrace trace : fTraces) {
    		trace.dispose();
    	}
    	fTraces = null;
    	fCheckpoints.clear();
        super.dispose();
    }

