    /**
     * Cancel the request.
     */
    public void cancel() {
        synchronized(lock) {
            fRequestCanceled = true;
            fRequestCompleted = true;
            lock.notify();
        }
    }

