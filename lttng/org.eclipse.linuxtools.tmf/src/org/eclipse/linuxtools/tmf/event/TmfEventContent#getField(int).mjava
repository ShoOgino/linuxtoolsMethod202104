	/**
	 * @param id
	 * @return
	 */
	public TmfEventField getField(int id) {
        assert id >= 0 && id < fNbFields;
        if (fFields == null) {
            fFields = fFormat.parse(fContent);
        }
		return fFields[id];
	}

