    /**
     * Copy constructor
     * 
     * @param other
     */
    public TmfTimestamp(TmfTimestamp other) {
    	assert(other != null);
        fValue = other.fValue;
        fScale = other.fScale;
        fPrecision = other.fPrecision;
    }

