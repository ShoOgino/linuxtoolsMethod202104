	@TmfSignalHandler
    public void handleTraceOpen(TmfTraceOpenedSignal signal) {
        ITmfTrace trace = signal.getTrace();
        if (trace == this) {
            indexTrace(false);
        }
    }

