    public TmfTrace_old(String id, ITmfTrace stream, TmfTimestamp epoch) {
        assert stream != null;
        fTraceId = id;
        fStream = stream;
        fEpoch = epoch;
        TmfSignalManager.addListener(this);
    }

