	/**
	 * Sets the absolute mask value.
	 * @param newValue	the new value of this mask
	 */
	public void setMaskValue(int newValue)
	{
		if (_maskType == MANDATORY){
			//FIXME: dirty hack because mandatory masks aren't supposed
			// to be set manually, and default to 0, when the default
			// value oprofile is expecting may be different. The problem
			// is there's no good place to set it, since the value is 
			// parsed from the XML output and there's no guarantee when
			// it will be parsed.
			_mask = _defaultMask;
		} else {
			_mask = newValue;
		}
	}

