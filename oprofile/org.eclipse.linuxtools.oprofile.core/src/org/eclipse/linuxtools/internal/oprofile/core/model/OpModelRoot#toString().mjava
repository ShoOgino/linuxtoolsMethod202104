	@Override
	public String toString() {
		String s = ""; //$NON-NLS-1$
		if (events != null) {
			for (int i = 0; i < events.length; i++) {
				if (events[i] != null) {
					s += "Event: "; //$NON-NLS-1$
					s += events[i].toString("\t"); //$NON-NLS-1$
				}
			}
		}
		return s;
	}

