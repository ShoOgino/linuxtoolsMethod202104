	@Override
	public String toString() {
		String s = ""; //$NON-NLS-1$
		if (_events != null) {
			for (int i = 0; i < _events.length; i++) {
				if (_events[i] != null) {
					s += "Event: "; //$NON-NLS-1$
					s += _events[i].toString("\t"); //$NON-NLS-1$
				}
			}
		}
		return s;
	}

