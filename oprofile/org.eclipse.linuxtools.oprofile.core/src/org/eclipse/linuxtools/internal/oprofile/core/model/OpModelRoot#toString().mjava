	@Override
	public String toString() {
		String s = ""; //$NON-NLS-1$
		if (session != null) {
			for (int i = 0; i < session.length; i++) {
				if (session[i] != null) {
					s += "Session: "; //$NON-NLS-1$
					s += session[i].toString("\t"); //$NON-NLS-1$
				}
			}
		}
		return s;
	}

