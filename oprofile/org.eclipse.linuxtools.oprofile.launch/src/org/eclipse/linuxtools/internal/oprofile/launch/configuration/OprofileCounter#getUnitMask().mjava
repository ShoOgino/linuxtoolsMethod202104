    public OpUnitMask getUnitMask() {
        OpEvent event = daemonEvent[0].getEvent();

        if (event != null) {
            return event.getUnitMask();
        } else {
            return null;
        }
    }

