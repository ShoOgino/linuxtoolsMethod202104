	@Override
	public String toString() {
		return "Image: id=" + id() + "\n" + "  parentId=" + parentId() + "\n"
				+ "  created=" + created() + "\n" + "  repoTags="
				+ repoTags().toString() + "\n" + "  size=" + size() + "\n"
				+ "  virtualSize=" + virtualSize() + "\n";
	}

