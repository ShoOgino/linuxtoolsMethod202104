	/**
	 * Full constructor.
	 * @param name
	 *            the repository name
	 * @param layer
	 *            the layer
	 */
	public RepositoryTag(final String name, final String layer) {
		this.layer = layer;
		this.name = name;
	}

