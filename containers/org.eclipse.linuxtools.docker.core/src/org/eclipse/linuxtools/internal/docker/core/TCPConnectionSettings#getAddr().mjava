	/**
	 * @return the ip address for the host
	 */
	public String getAddr() {
		if (ipaddr == null) {
			Matcher m = ipaddrPattern.matcher(host);
			if (m.matches()) {
				ipaddr = m.group(1);
			} else {
				ipaddr = "";
			}
		}
		return ipaddr;
	}

