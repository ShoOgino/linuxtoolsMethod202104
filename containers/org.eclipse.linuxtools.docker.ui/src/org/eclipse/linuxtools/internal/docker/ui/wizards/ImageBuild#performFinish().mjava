	@Override
	public boolean performFinish() {
		imageName = mainPage.getImageName();
		directory = new Path(mainPage.getDirectory());

		try {
			lines = numberOfLines();
		} catch (IOException e) {
			// do nothing
		}

		return true;
	}

