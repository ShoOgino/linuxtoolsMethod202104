        /*
         * @see Iterator#next()
         */
        @Override
        public Annotation next() {
            try {
                return fNext;
            } finally {
                if (fIterator != null)
                    skip();
            }
        }

