    XMLParser() {
        id = 0;
        currentlyIn = 0;
        if (keyValues != null) {
            keyValues.clear();
        }
        keyValues = new HashMap<>();

        if (idList != null) {
            idList.clear();
        }
        idList = new ArrayList<>();

        textMode = false;
    }

