    /**
     * Delete the log at File and replace it with a new (empty) file
     * @param log
     */
    public static void deleteLog(File log) {
        log.delete();
        try {
            log.createNewFile();
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

