	public GraphLabel(GraphCanvas canvas, String title, Point center, float size, int style) {
		this.canvas = canvas;
		this.title = title;
		this.center = center;
		this.size = size;
		this.style = style;
		this.parent = null;
	}

