    @Override
    public Object execute(ExecutionEvent event) {
        if (active) {
            new Thread(new Runnable() {
                @Override
                public void run() {
                    view.refresh();
                }
            }).start();
        }
        return null;
    }

