    /**
     * Refreshes the list of probe aliases in the viewer.
     */
    @Override
    public void refresh() {
        tree = TapsetLibrary.getProbes();
        if (tree != null) {
            viewer.setInput(tree);
        }
    }

