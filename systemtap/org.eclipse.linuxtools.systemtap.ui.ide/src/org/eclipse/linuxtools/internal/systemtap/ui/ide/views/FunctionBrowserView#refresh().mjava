    /**
     * Refreshes the list of functions in the viewer.
     */
    @Override
    public void refresh() {
        tree = TapsetLibrary.getFunctions();
        if (tree != null) {
            viewer.setInput(tree);
        }
    }

