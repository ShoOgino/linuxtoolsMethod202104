	private boolean isTokenDelimiter(char c) {
		if (Character.isSpaceChar(c)) {
			return true;
		}

		switch (c) {
		case '\n':
		case '\0':
		case ',':
		case '{':
		case '}':
			return true;
		}
		return false;
	}

