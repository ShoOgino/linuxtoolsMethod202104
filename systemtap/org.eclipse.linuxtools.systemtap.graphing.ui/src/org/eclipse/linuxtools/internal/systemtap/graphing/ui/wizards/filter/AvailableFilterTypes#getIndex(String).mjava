    private static int getIndex(String id) {
        for(int i=0; i< FILTER_IDS.length; i++) {
            if(id.equals(FILTER_IDS[i])) {
                return i;
            }
        }
        return -1;
    }

