        @Override
        public boolean test() {
            if (!exact) {
                return table.rowCount() >= expectedRows;
            } else {
                return table.rowCount() == expectedRows;
            }
        }

