    @Test
    public void testForeachStatement() throws Exception {
        assertIndenterResult(
                "foreach (i+ in arr) {\n" +
                " k = 7",

                "foreach (i+ in arr) {\n" +
                "\tk = 7");
    }

