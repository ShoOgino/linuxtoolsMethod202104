    @Test
    public void testWrappedFor_1_Bug277625() throws Exception {
        assertIndenterResult(
                "for (int i = 0;\n" +
                "i < 2; i++)",

                "for (int i = 0;\n" +
                "\t\ti < 2; i++)");
    }

