	public void dispose() {
		if(null != outputFile && !saveLog)
			outputFile.delete();
		outputFile = null;
		if(null != writer) {
			try {
				writer.close();
			} catch(IOException ioe) {}
		}
		writer = null;
	}

