	public void testScriptExecution(){

		String scriptPath = location + "simple.stp";
		
		//RUN
		stapgen
		.generateCommand(
				scriptPath,
				"",
				"",
				false,
				false,
				"", "");
		
		assertEquals(
				"stap "+scriptPath,
				stapgen.getExecuteCommand());
		//END
	}

