	/**
	 * Calculates the number of used slots.
	 */
	private void calculateUsedSlots()
	{
		int count = 0;
		for (int i = 0; i < external.length; i++)
			if(external[i] != null)
				count++;
		usedSlotCount = count;
	}

