	public void dispose() {
		if(!disposed) {
			disposed = true;
			data = null;
			if(null != outputData)
				outputData.delete(0, outputData.length());
			outputData = null;
			parser = null;
		}
	}

