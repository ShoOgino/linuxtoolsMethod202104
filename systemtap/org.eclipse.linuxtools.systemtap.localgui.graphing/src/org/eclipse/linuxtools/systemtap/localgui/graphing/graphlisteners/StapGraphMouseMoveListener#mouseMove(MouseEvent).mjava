	@Override
	public void mouseMove(MouseEvent e) {
		//-------------Panning
			
		if (!stop) {	
			int yDiff, xDiff;
			xDiff = prevX - e.x;
			yDiff = prevY - e.y;
			
			graph.scrollSmoothBy(xDiff, yDiff);
	
			prevX = e.x;
			prevY = e.y;
		}
	}

