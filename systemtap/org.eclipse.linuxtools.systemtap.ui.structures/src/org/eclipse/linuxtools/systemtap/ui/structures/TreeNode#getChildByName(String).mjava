	public TreeNode getChildByName(String name){
		int n = getChildCount();

		for (int i = 0; i < n; i++) {
			if (getChildAt(i).toString().equals(name))
				return getChildAt(i);
		}

		return null;
	}

