		@Override
		public void streamAppended(String text, IStreamMonitor monitor) {
			try {
				counter++;
				if (counter < 300)
					h.appendToExistingFile(text);
			} catch (IOException e) {
				e.printStackTrace();
			}
			
		}

