	@Override
	public void launch(ISelection selection, String mode) {
		super.Init();
		completed = false;
		promptForInputs();
		
		this.mode = mode; 
		
//		finishLaunch(scriptPath + ": " + binName, mode); //$NON-NLS-1$
	}

