	public void testScriptExecution(){

		String scriptPath = location + "simple.stp";
		
		//RUN
		stapact
		.generateCommand(
				scriptPath,
				"",
				"",
				false,
				false,
				"", "");
		
		assertEquals(
				"stap "+scriptPath,
				stapact.getExecuteCommand());
		//END
	}

