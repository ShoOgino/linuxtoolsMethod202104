    public TreeNode(Object d, boolean c) {
        children = new ArrayList<>();
        data = d;
        clickable = c;

        if(null == data) {
            display = null;
        } else {
            display = d.toString();
        }
    }

