    public TreeNode(Object d, boolean c) {
        children = new ArrayList<>();
        data = d;
        clickable = c;

        if (data == null) {
            display = null;
        } else {
            display = d.toString();
        }
    }

