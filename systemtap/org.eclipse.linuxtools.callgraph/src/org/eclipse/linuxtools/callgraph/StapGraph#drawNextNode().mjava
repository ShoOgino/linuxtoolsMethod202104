	public void drawNextNode() {
		if (isCollapseMode()) {
			setCollapseMode(false);
		}
		int toDraw = getNextCalledNode(getRootVisibleNodeNumber());
		if (toDraw != -1)
			draw(toDraw);
		 else
			proj.pause();
	}

