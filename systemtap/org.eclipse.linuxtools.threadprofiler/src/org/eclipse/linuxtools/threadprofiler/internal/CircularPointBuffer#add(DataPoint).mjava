	public void add(DataPoint p) {
		buffer[tail] = p;
		tail++;
		if (tail >= buffer.length) {
			tail = 0;
		} 
		if (tail == head) {
			head++;
			if (head >= buffer.length) {
				head = 0;
			}
		}
	}

