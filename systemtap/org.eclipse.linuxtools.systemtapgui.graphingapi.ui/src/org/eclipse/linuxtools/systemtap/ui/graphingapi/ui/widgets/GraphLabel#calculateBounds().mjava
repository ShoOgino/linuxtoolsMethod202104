	public void calculateBounds() {
		if(null != parent) {
			Point size = parent.getSize();
			x = (size.x - width)>>1;
			y = parent.getBounds().y;
		} else {
			x = center.x - (width>>1);
			y = center.y - (height>>1);
		}
	}

