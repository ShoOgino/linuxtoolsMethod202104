	/**
	 * Disposes of all internal references in the class. No method should be called after this.
	 */
	public void dispose() {
		if(!disposed) {
			disposed = true;
			output = null;
			console = null;
			msgConsole = null;
		}
	}

