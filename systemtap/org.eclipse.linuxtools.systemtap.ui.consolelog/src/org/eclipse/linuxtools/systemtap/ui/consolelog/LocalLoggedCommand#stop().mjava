	@Override
	public synchronized void stop() {
		if(!stopped) {
			process.destroy();
			stopped = true;
			if(null != errorGobbler)
				errorGobbler.stop();
			if(null != inputGobbler)
				inputGobbler.stop();
		}
	}

