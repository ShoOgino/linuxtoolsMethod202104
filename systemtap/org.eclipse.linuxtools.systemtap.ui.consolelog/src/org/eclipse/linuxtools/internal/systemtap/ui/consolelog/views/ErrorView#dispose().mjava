    /**
     * Disposes of everything in this class.
     */
    @Override
    public void dispose() {
        if(null != table)
            table.dispose();
        table = null;
        super.dispose();
    }

