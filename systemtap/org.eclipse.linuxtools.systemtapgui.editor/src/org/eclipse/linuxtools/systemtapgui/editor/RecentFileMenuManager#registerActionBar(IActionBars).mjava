	public void registerActionBar(IActionBars bars) {
		addedBars.add(bars);
		
		for(int i=0; i<MAX_RECENT_FILES; i++)
			bars.setGlobalActionHandler(
					"org.eclipse.linuxtools.systemtapgui.editor.actions.file.openRecentFile" + i,
					fileActions[i]);
		bars.updateActionBars();
	}

