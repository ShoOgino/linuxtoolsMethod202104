    /**
     * Constructor.
     * 
     * @param dataSet
     *            data for chart
     */
    
    public DialChartBuilder(IAdapter adapter, Composite parent, int style) {
    	 super(adapter, parent, style);
	}

