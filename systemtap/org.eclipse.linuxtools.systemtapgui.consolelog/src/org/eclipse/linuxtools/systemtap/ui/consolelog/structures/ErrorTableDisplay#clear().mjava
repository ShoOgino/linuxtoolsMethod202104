	/**
	 * Clears all items from the table.
	 */
	public void clear() {
		table.getDisplay().syncExec(new Runnable() {
			boolean stop = false;
			public void run() {
				if(stop) return;
				try {
					table.removeAll();
				} catch (Exception e) {
					stop = true;
				}
			}
			
		});
	}

