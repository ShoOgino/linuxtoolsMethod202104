	/**
	 * @param cm
	 *            Chart Model
	 */
	public void renderModel( Chart cm )
	{
		 this.cm = cm;

		if ( preview != null && !preview.isDisposed( ) )
		{
			updateBuffer( );
			preview.redraw( );
		}
	}

