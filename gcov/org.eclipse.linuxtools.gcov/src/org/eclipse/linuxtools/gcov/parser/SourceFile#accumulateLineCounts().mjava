	public void accumulateLineCounts() {
		for (Line line : lines) {
			if (line.isExists()) {
				cvrge.incLinesInstrumented();
				if (line.getCount() != 0)
					cvrge.incLinesExecuted();
			}
		}
	}

