	/** gets how many STColumn exist
	 * 
	 * @return
	 */
	public int getColumnCount(){
		if (columns == null){
			columns = getColumns();
		}
		return columns!=null ? columns.size() : 0;
	}

