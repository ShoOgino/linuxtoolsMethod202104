	public void testReleaseTag() {
		String testText = "Release: blah";
		
		try {
			newFile(testText);
			
			assertEquals("blah", specfile.getRelease());
		} catch (Exception e) {
			fail();
		}
		
	}

