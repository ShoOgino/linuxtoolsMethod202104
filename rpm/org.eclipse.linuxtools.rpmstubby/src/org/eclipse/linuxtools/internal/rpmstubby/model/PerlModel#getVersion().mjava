    /**
     * Returns the version
     *
     * @return The version
     */
    public String getVersion() {
        String version = getValue(CommonMetaData.VERSION);
        if (!hasDigits(version)) {
            version = "1 " + FIX_ME;
        }
        return version;
    }

